apiVersion: autoscaling/v2
kind: HorizontalPodAutoscaler
metadata:
  name: eurotransit-inventory
  namespace: {{ .Release.Namespace }}
  labels:
    app.kubernetes.io/name: eurotransit-inventory
    app.kubernetes.io/instance: {{ .Release.Name }}
    {{- include "eurotransit.commonLabels" . | nindent 4 }}
spec:
  scaleTargetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: eurotransit-inventory
  minReplicas: {{ .Values.inventory.hpa.minReplicas }}
  maxReplicas: {{ .Values.inventory.hpa.maxReplicas }}
  metrics:
    - type: Resource
      resource:
        name: cpu
        target:
          type: Utilization
          averageUtilization: {{ .Values.inventory.hpa.targetCPUUtilizationPercentage }}
