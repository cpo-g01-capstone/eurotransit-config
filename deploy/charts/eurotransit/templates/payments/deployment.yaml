apiVersion: apps/v1
kind: Deployment
metadata:
  name: eurotransit-payments
  namespace: {{ .Release.Namespace }}
  labels:
    app.kubernetes.io/name: eurotransit-payments
    app.kubernetes.io/instance: {{ .Release.Name }}
    {{- include "eurotransit.commonLabels" . | nindent 4 }}
spec:
  # spec.replicas intentionally omitted: the payments HPA owns the replica count.
  # Pinning it here makes Argo CD selfHeal fight HPA scale-out (ADR 0023).
  selector:
    matchLabels:
      app.kubernetes.io/name: eurotransit-payments
      app.kubernetes.io/instance: {{ .Release.Name }}
  template:
    metadata:
      labels:
        app.kubernetes.io/name: eurotransit-payments
        app.kubernetes.io/instance: {{ .Release.Name }}
        # Grants the pod the NetworkPolicy selector (adversarial-audit fix, #52):
        # the policies select part-of, which commonLabels puts on the
        # Deployment but NOT on the pod template — without this line every
        # policy silently matched zero pods.
        app.kubernetes.io/part-of: eurotransit
    spec:
      terminationGracePeriodSeconds: {{ .Values.lifecycle.terminationGracePeriodSeconds }}
      {{- include "eurotransit.topologySpread" (dict "name" "eurotransit-payments" "instance" .Release.Name "hard" true) | nindent 6 }}
      {{- if .Values.global.imagePullSecrets }}
      imagePullSecrets:
        {{- toYaml .Values.global.imagePullSecrets | nindent 8 }}
      {{- end }}
      containers:
        - name: payments
          image: {{ include "eurotransit.imageRef" (list .Values.global.imageRegistry .Values.payments.image) }}
          imagePullPolicy: {{ .Values.payments.image.pullPolicy }}
          ports:
            - name: http
              containerPort: 8080
              protocol: TCP
          env:
            - name: SPRING_PROFILES_ACTIVE
              value: "kubernetes"
            # Pin to the chart's standard http port (see catalog/deployment.yaml).
            - name: SERVER_PORT
              value: "8080"
            # DB env-var names must match payments-service application.yml (PAYMENTS_DB_*).
            # R2DBC at runtime; a separate JDBC URL for Flyway (JDBC-only). Wrong
            # names -> the app silently falls back to localhost:5432 and crashes.
            - name: PAYMENTS_DB_R2DBC_URL
              value: "r2dbc:postgresql://{{ .Values.payments.db.host }}:{{ .Values.payments.db.port }}/{{ .Values.payments.db.name }}"
            - name: PAYMENTS_DB_JDBC_URL
              value: "jdbc:postgresql://{{ .Values.payments.db.host }}:{{ .Values.payments.db.port }}/{{ .Values.payments.db.name }}"
            - name: PAYMENTS_DB_USERNAME
              valueFrom:
                secretKeyRef:
                  name: {{ .Values.payments.db.secretName }}
                  key: username
            - name: PAYMENTS_DB_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: {{ .Values.payments.db.secretName }}
                  key: password
            - name: SPRING_KAFKA_BOOTSTRAP_SERVERS
              value: {{ .Values.kafka.bootstrapServers | quote }}
            - name: SPRING_KAFKA_CONSUMER_GROUP_ID
              value: "payments-group"
            # Spring graceful shutdown — drain in-flight work before the JVM exits.
            - name: SPRING_LIFECYCLE_TIMEOUT_PER_SHUTDOWN_PHASE
              value: {{ .Values.lifecycle.shutdownTimeout | quote }}
          {{- include "eurotransit.preStop" . | nindent 10 }}
          {{- include "eurotransit.probes" . | nindent 10 }}
          resources:
            {{- toYaml .Values.payments.resources | nindent 12 }}
